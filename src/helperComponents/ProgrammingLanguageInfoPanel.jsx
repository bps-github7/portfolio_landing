import { Component } from 'react';

export default class ProgrammingLanguageInfoPanel extends Component {
  constructor(props) {
    super(props);
    this.state = {  }
  }
  render() { 
    return (
      // On hover over the text item title, display a tool tip with additional info.
      // optionally, a button to press that triggers this -> 'more info...'
      <div>
        Hello Kiddies
      </div>
    );
  }
}