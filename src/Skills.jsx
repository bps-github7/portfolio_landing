import React, { Component } from 'react';

export default class Skills extends Component {
  constructor(props) {
    super(props);
    this.state = {  }
  }
  render() { 
    return (
      <div class="app-skills">
        Use grid to list tiles with heading and sub text about what you can do with each skill
      </div>
      );
  }
};